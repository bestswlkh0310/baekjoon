fun main() {
    val n = readln().toInt()
    for (k in 0 until n) {
        //var (a, b) = readln().split(" ").map { it.toInt() }
        println("yes")
  /*      var sum = 0

        loop@
        for (i in 1 .. a / 2) {
            for (j in 3 .. a / 2 + 3 step 2) {
                if (a == 1) break@loop
                if (a % 2 == 0) {
                    a /= 2
                    sum += 2
                }
                if (a % j == 0) {
                    a /= j
                    sum += j
                }
            }
        }
        if (sum == b) println("yes")
        else println("no")*/
    }
}